# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Calculator' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Calculator
  pod 'CalculatorKeyboard', :git => 'https://github.com/ScientificDimension/CalculatorKeyboard', :branch => 'develop'
  pod 'SwiftyStateMachine', :git => 'https://github.com/ScientificDimension/SwiftyStateMachine', :branch => 'develop'

end
